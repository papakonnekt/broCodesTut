public class stringmethodtut {

    public static void main(String[] args) {
        // String is a reference data type that can store one or more characters

        //start by typing String (to declare your making a string,) followed by naming it, here we are naming it name
        String name = "rick";

        //we have named the string rick, next we are seeing if the name in the result is equal to rick.
        // boolean result = name.equals("kick");

        //this will tell you the numerical length of the string, so our string named "name" will only be 4, because rick is 4 letters
        int result = name.length();




        System.out.println(result);
    }

}